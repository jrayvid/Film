<%= form_for [@band, @video] do |f| %>


  <div class="form-inputs">
    <%= f.text_field :title, placeholder: :title %>
    
    <%= f.text_field :url, placeholder: "URL" %>

  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>

<% end %>
