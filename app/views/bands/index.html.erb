<%= render 'layouts/usernavigation' %>

<h1 class="text-center text-uppercase">band's page</h1>


<% @bands.each do |b| %>
<% link_to b.bname, band_path(b)%>
<div class="col-md-4">
<% b.id %><br>
<%= image_tag b.avatar.url :medium %>

<%= link_to b.bname, band_path(b)%>
</div>
<% end %>
